[package]
name = "proto-ghostty-vt"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
libloading.workspace = true
thiserror.workspace = true

[lints.rust]
# All unsafe code in Forge's Ghostty integration is isolated in this FFI crate.
unsafe_code = "allow"

[lints.clippy]
all = "warn"
pedantic = "warn"

